# Stage 10｜AI Architecture

状态：LOCKED

## 能力定位

设计 AI Native 系统的责任边界：哪些由模型判断，哪些必须由确定性系统约束，如何评测、降级和追责。

## 打开条件

同一套架构判断已经能在非 AI 系统上独立走完：对象、约束、方案比较、验证、推翻。然后再迁移到“模型是组件”的系统。

不要把本阶段当成最后两章才学的新学科。它是终局形态，但必须从当前边缘长过去。
