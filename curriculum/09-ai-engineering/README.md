# Stage 09｜AI Engineering

状态：LOCKED

## 能力定位

把模型、评测、数据、工具调用和反馈闭环做成可运行系统，而不是会调用一次 API。

## 打开条件

出现真实 AI 产品约束，并且当前主模糊点（现在必须定 / 必须延后）已经变 Sharp 后再进入。

当前可以做的唯一事：在 API 设计题中偶尔探测“如果多一个 AI 客户端会不会改变契约”，不单独开课。
