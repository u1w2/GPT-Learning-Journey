# Stage 03｜Software Architecture

状态：LOCKED

## 1. 能力定位

在系统边界已经清楚后，设计模块、分层、依赖方向和可替换性。

## 2. 为什么需要

当 API 和核心对象稳定后，内部结构会决定后续改动成本。现在还没有进入实现，因此本阶段不展开。

## 3. 前置能力

Stage 02 中至少完成 API 契约判断，并开始有项目证据。

## 4-11. 打开条件

出现以下真实约束后再打开：

- 模块边界开始影响交付
- 出现循环依赖或改一处坏一片
- 需要明确领域对象与接口适配

未出现这些约束前，禁止为了“架构完整”先画分层大图。
