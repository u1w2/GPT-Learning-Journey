# CURRENT STATE｜跨对话学习状态

> 本文件是学习系统的当前状态快照。新的 ChatGPT 对话、AI Agent 或其他学习助手开始工作时，应优先读取本文件，再按需读取其他 Learner State、当前项目和 Experience。

## 1. 恢复协议

当用户说：

> 继续 GPT-Learning-Journey / 继续我们的成长任务 / 继续我们的学习

执行：

1. 读取本文件。
2. 确认当前阶段、当前任务和当前项目。
3. 读取 `learner/progress.md`、`capability.md`、`weak-points.md`、`mistakes.md` 中与当前任务相关的内容。
4. 读取当前项目 README、最近实验结果和未解决问题。
5. 检查最近一次 session，确认上次实际停在哪里。
6. 不重复已经完成的内容，直接从当前未完成节点继续。
7. 如果状态文件与项目记录发生冲突，以最新且有明确完成证据的记录为准，并指出冲突。
8. 完成一个有意义的学习节点后更新 Learner State。

## 2. 当前阶段

- Stage: 02-system-design
- Phase: API Design
- Task: API Question 1 — 第一阶段 API 必须提前确定哪些边界，避免后续客户端、后台和第三方接入导致兼容与重构
- Project: 架构师成长训练主线
- Status: IN_PROGRESS

## 3. 当前目标

把已经具备的业务、数据建模和扩展性直觉，训练成稳定的 API 架构决策方法：能够从客户端演进、兼容性、资源建模、错误处理、版本策略、安全和可观测性等角度主动发现 API 的长期约束。

## 4. 已完成

- 已完成一次当前能力盘点：当前能力定位为“高级工程师 → 初级架构师”区间，架构思维已经开始形成，但理论体系、工程化、分布式、可靠性和结构化表达仍需补齐。
- 已讨论 `user → user_order → order` 关联模型，并主动考虑冗余字段、查询压力、数据规模、区域划分、未来分布式以及时间/空间取舍。
- 已进入 API 设计训练，并明确要求：只回答当前问题，不提前展开后续知识点。

## 5. 当前进行中

API 设计基础：第一阶段就必须确定的 API 契约与演进边界。

当前只回答：

> 第一阶段为了快速验证，前端、小程序、后台管理端都需要调用 API。API 哪些东西第一阶段就必须确定好，避免后面前端、客户端、第三方接入越来越多以后出现大量兼容和重构？

训练规则：先由学习者独立作答；AI 负责追问、指出遗漏和风险，不直接替代判断。

## 6. 当前能力状态

### 已观察到的优势

- 能从业务对象和访问关系出发思考数据模型。
- 会主动考虑数据规模增长后的查询压力。
- 已经开始使用时间/空间取舍分析方案，而不是只寻找“标准答案”。
- 能提前考虑未来分布式演进，而不是只考虑当前单体规模。
- 能把系统设计问题从“怎么实现”提升到“以后会不会重构”。

### 待验证能力

- API 契约设计是否完整。
- 兼容性与版本演进是否有系统方法。
- 一致性、幂等、错误模型、分页、鉴权和可观测性是否能主动纳入设计。
- 能否用统一的架构决策框架解释“为什么这样设计”。

## 7. 当前短板

1. 架构直觉已经出现，但还没有完全形成可重复的架构方法论。
2. 分布式、可靠性、缓存、消息队列、可观测性和工程治理需要通过真实项目继续补齐。
3. 需要把“我觉得以后可能需要”训练成“在什么条件下需要、替代方案是什么、代价是什么”。
4. 需要强化结构化架构表达与面试答辩能力。

## 8. 未解决问题

- API 第一阶段最小稳定契约应该包含哪些内容？
- 哪些设计应该现在确定，哪些应该刻意延后？
- 如何避免为了“未来扩展”过度设计？

## 9. 最近一次学习结果

2026-08-16：完成当前能力盘点。核心结论：继续训练重点应从“知识点学习”转向“真实问题 → 方案 → Trade-off → 风险 → 实验/验证 → 复盘 → 经验迁移”。随后进入 API 设计题。

## 10. 下一步唯一主任务

完成 API Question 1 的独立设计回答。回答完成后再由 AI 进行架构师级 Review，并把暴露出的缺口记录到 Learner State。

## 11. AI 工作约束

- 不因新对话而重新开始课程。
- 不把“读过资料”视为“掌握能力”。
- 优先通过项目、实验、问题复现和答辩判断能力。
- 优先解决当前短板，而不是无目的扩展知识。
- 发现新的能力缺口时记录到 Learner State。
- 重要项目经验必须沉淀到 `experience/`。
- 重要问题必须能追溯到项目、实验和解决方案。
- 阶段切换必须有明确验收证据。
- 训练题默认先让学习者独立判断；除非学习者要求直接答案，否则不要提前替代作答。

## 12. 状态更新规则

本文件只保存当前快照，不承担完整历史记录。历史放在 `learner/sessions/`、项目记录和 Experience 中。

每次发生以下情况之一，应更新本文件：

- 当前任务变化
- 项目阶段变化
- 能力评估变化
- 新增重要短板
- 新增重要错误
- 完成关键实验
- 形成重要项目经验
- 阶段通过或需要返工
