# Constraint Increment｜约束递增

不要一开始就上：

```text
微服务
K8s
Kafka
Redis Cluster
分库分表
Service Mesh
```

正确顺序：

```text
单体
 ↓
数据库
 ↓
缓存
 ↓
并发
 ↓
消息
 ↓
服务拆分
 ↓
分布式
 ↓
高可用
 ↓
容灾
```

## 每次引入复杂度之前必须问

> 为什么现在需要？

如果没有真实约束：

> 不要引入复杂度。

## 证据

“以后可能会用到”不是约束。
“当前 QPS 已经打满数据库，实验已经复现”才是约束。
